{
  "name": "mstrlaw",
  "private": true,
  "scripts": {
    "start": "nodemon --watch index.js --watch styles/app.scss",
    "build": "rm -rf build/ && node index.js",
    "generate": "rm -rf build/ && node index.js",
    "serve": "serve build"
  },
  "devDependencies": {
    "@metalsmith/markdown": "^1.3.0",
    "@metalsmith/permalinks": "^2.3.0",
    "jstransformer-handlebars": "^1.1.0",
    "metalsmith": "^2.3.0",
    "metalsmith-browser-sync": "^1.1.1",
    "metalsmith-collections": "^0.9.0",
    "metalsmith-discover-partials": "^0.1.2",
    "metalsmith-layouts": "^2.3.1",
    "nodemon": "^2.0.15",
    "serve": "^13.0.2"
  },
  "dependencies": {
    "metalsmith-sass": "^1.7.0"
  }
}
